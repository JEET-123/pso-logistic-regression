# pso-logistic-regression
PSO-LR is an open-source, sklearn-compatible implementation of    Metaheuristic-Optimized Logistic Regression (MOLR), where model parameters  are estimated using **Particle Swarm Optimization (PSO) instead of  gradient-based solvers.
